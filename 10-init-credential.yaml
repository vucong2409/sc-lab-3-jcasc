credentials:
  system:
    domainCredentials:
      - credentials:
        - usernamePassword:
            scope: GLOBAL
            id: "git-cred"
            username: "${GIT_USERNAME}"
            password: "${GIT_PASSWORD}"

